@model IEnumerable<RhondasSouthernScentsCandleStore.Models.Candle>
@helper Truncate(string input, int length)
{
if (input.Length <= length)
{
        @input
}
else
{
        @input.Substring(0, length)<text>...</text>
}
}


@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CandleCategory.CategoryName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Scent.ScentName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CandleDesc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.CandleImgUrl)
        </th>*@
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CandleCategory.CategoryName)
        </td>
        <td>
            @*@Html.DisplayFor(modelItem => item.Scent.ScentName)*@
            @Truncate(item.Scent.ScentName, 25)
        </td>
        <td>
            @*@Html.DisplayFor(modelItem => item.CandleDesc)*@
            @Truncate(item.CandleDesc, 25)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.CandleImgUrl)
        </td>*@
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CandleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CandleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CandleID })
        </td>
    </tr>
}

</table>
